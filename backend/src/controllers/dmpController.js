/**
 * DMP Battery Data Bridge — Controller
 *
 * NOTE: All DMP business logic (MDB file reading, stats computation,
 * Excel report generation) has been moved to the Python client service
 * at hardware-services/dmp_service.py
 *
 * The Node.js backend acts as a proxy only.
 * See backend/src/routes/dmp.js for the proxy implementation.
 */

// This file is intentionally empty — logic lives in dmp_service.py
module.exports = {};
